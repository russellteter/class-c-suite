{
  "name": "@c-suite/main",
  "version": "0.0.0",
  "dependencies": {
    "@c-suite/shared-types": "workspace:*",
    "@c-suite/vault-watcher": "workspace:*",
    "better-sqlite3": "^12.10.0",
    "electron": "^38.2.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^12.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @c-suite/utility build && tsc && VITE_DEV=1 NODE_ENV=development electron .",
    "dev:full": "pnpm --filter @c-suite/utility build && tsc && VITE_DEV=1 NODE_ENV=development electron .",
    "typecheck": "tsc"
  }
}
